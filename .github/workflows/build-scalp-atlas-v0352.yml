name: Build SCALP ATLAS v0.3.52 APK

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/build-scalp-atlas-v0352.yml'
      - '.github/scripts/patch_scalp_atlas_v0352.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Apply all Scalp Atlas patches through v0.3.52
        run: |
          set -e
          for f in $(ls .github/scripts/patch_scalp_atlas_v03*.py | sort -V); do
            echo "Applying $f"
            python3 "$f"
          done

      - name: Apply v0.3.52 visual title and Android version
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import json

          app = Path('scalp-atlas-new/App.tsx')
          text = app.read_text(encoding='utf-8')

          old_title = '<Text style={styles.logo}>SCALP ATLAS</Text>'
          new_title = "<Text style={styles.logo}><Text style={{ color: '#20E0FF' }}>SC</Text><Text style={{ color: '#28BFFF' }}>AL</Text><Text style={{ color: '#557FFF' }}>P </Text><Text style={{ color: '#8758FF' }}>AT</Text><Text style={{ color: '#C43AF2' }}>LA</Text><Text style={{ color: '#FF2DB8' }}>S</Text></Text>"
          if old_title not in text:
              raise SystemExit('Titlul SCALP ATLAS nu a fost găsit; build oprit pentru siguranță.')
          text = text.replace(old_title, new_title, 1)

          old_style = "logo: { color: '#f7fafc', fontSize: 25, fontWeight: '900', letterSpacing: 1.4 },"
          new_style = "logo: { color: '#f7fafc', fontSize: 25, fontWeight: '900', letterSpacing: 1.4, textShadowColor: 'rgba(139,92,246,0.48)', textShadowOffset: { width: 0, height: 0 }, textShadowRadius: 5 },"
          if old_style not in text:
              raise SystemExit('Stilul titlului nu a fost găsit; build oprit pentru siguranță.')
          text = text.replace(old_style, new_style, 1)
          app.write_text(text, encoding='utf-8')

          cfg_path = Path('scalp-atlas-new/app.json')
          cfg = json.loads(cfg_path.read_text(encoding='utf-8'))
          cfg['expo']['version'] = '0.3.52'
          cfg['expo']['android']['versionCode'] = 62
          cfg_path.write_text(json.dumps(cfg, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')
          PY

      - name: Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: platform-tools

      - name: Install dependencies
        working-directory: scalp-atlas-new
        run: npm install

      - name: Generate clean launcher icon
        run: |
          set -e
          sudo apt-get update -qq
          sudo apt-get install -y -qq librsvg2-bin
          rsvg-convert -w 1024 -h 1024 scalp-atlas-new/assets/app-icon.svg -o scalp-atlas-new/assets/app-icon.png

      - name: Generate Android project
        working-directory: scalp-atlas-new
        env:
          CI: '1'
        run: npx expo prebuild --platform android --clean

      - name: Build standalone release APK
        working-directory: scalp-atlas-new/android
        env:
          NODE_ENV: production
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Collect APK
        run: cp scalp-atlas-new/android/app/build/outputs/apk/release/app-release.apk ScalpAtlas-v0.3.52-NOISY-CHANNEL-FILTER.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ScalpAtlas-v0.3.52-NOISY-CHANNEL-FILTER
          path: ScalpAtlas-v0.3.52-NOISY-CHANNEL-FILTER.apk
          if-no-files-found: error

      - name: Publish test APK release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete scalp-atlas-v0.3.52-test --cleanup-tag --yes || true
          gh release create scalp-atlas-v0.3.52-test \
            ScalpAtlas-v0.3.52-NOISY-CHANNEL-FILTER.apk \
            --target "$GITHUB_SHA" \
            --title "SCALP ATLAS v0.3.52 Noisy Channel Filter" \
            --notes "Corecție după EUR/NZD OTC M15: un canal geometric cu multe alternări și respingeri nu mai produce BUY/SELL direct dacă structura este prea zgomotoasă. Trecere în WAIT cu confirmarea ultimului maxim/minim structural. În UI, Probabilitate devine Scor semnal pentru BUY/SELL confirmat. Cele 70 de modele și protecțiile anterioare sunt păstrate." \
            --prerelease
